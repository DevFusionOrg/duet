// Encryption utilities removed. This module is intentionally left minimal.
export const encryptMessage = async (text) => text;
export const decryptMessage = async (text) => text;
export const initializeChatEncryption = async () => null;
export const deleteChatKey = async () => {};
